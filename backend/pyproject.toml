[project]
name = "ai-study-partner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "alembic>=1.18.4",
    "anthropic>=0.40.0",
    "bcrypt>=4.0.0",
    "fastapi>=0.128.0",
    "google-auth>=2.35.0",
    "langchain-google-genai>=4.2.0",
    "langchain-text-splitters>=1.1.0",
    "openai>=1.58.0",
    "pdfplumber>=0.11.9",
    "python-bidi>=0.4.2,<0.6.9",
    "pgvector>=0.4.2",
    "psycopg2-binary>=2.9.11",
    "pyjwt>=2.12.1",
    "python-docx>=1.2.0",
    "python-dotenv>=1.2.1",
    "python-multipart>=0.0.22",
    "sqlalchemy>=2.0.46",
    "uvicorn>=0.40.0",
    "pytest>=8.0.0",
    "httpx>=0.27.0",
    # F-036: render the AI-generated unified-summary markdown to a PDF
    # so the lecture viewer can open it inside MainWorkspace like any
    # other library document. `markdown` does md → HTML; `weasyprint`
    # renders HTML → PDF (uses cairo / pango which are already in the
    # backend image for libreoffice).
    "markdown>=3.6",
    "weasyprint>=63.0",
]
